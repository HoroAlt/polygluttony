// The maia shadcn components (e.g. sonner's Toaster) read the theme via
// `next-themes`, so we use it as the single source of truth for theming.
export { ThemeProvider, useTheme } from "next-themes";
